# 不要提交到 git！在 .gitignore 中添加 deploy/k8s/secret.yaml
#
# 生成 base64：echo -n 'your-value' | base64
apiVersion: v1
kind: Secret
metadata:
  name: forge-secret
  namespace: forge
type: Opaque
data:
  ANTHROPIC_API_KEY: REPLACE_WITH_BASE64_VALUE
  POSTGRES_PASSWORD: REPLACE_WITH_BASE64_VALUE
